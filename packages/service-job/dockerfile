FROM node:18-alpine
WORKDIR /app
COPY ../../package.json ../../yarn.lock ./
COPY . .
RUN yarn install --frozen-lockfile 
RUN yarn workspace service-job build
CMD ["yarn", "workspace", "service-job", "dev"]
